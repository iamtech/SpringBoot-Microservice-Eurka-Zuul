spring:
  profiles:
    active: development
  cloud:
    config:
      uri: http://localhost:8094
      
management:
  endpoints:
    web:
      exposure:
        include: refresh,health,info,hystrix.stream     